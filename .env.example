# Copy to .env.local for local development. Never commit a real .env* file (see .gitignore).

# PostgreSQL connection string used by Prisma (set up on Day 4).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_firm_management?schema=public"
